version: 2

models:
  - name: dim_customers
    description: Customer dimensions table
    access: public
    columns:
      - name: customer_key
        description: Primary key on the customers table
        tests:
          - unique
          - not_null
        meta:
          contains_pii: true
      - name: region
        description: region name
        tests:
          - accepted_values:
              values: ['AFRICA','MIDDLE EAST','ASIA','EUROPE','AMERICA']
              severity: warn
      - name: name
        description: customer id
      - name: address
        description: address of the customer
      - name: nation
        description: nation name
      - name: phone_number
        description: phone number of the customer
      - name: account_balance
        description: account balance for the customer
      - name: market_segment
        description: market segment of the customer

  - name: fct_orders
    description: orders fact table
    access: public
    columns:
      - name: order_key
        description: primary key of the model
        tests:
          - unique
          - not_null
      - name: customer_key
        description: foreign key for customers
        tests:
          - relationships:
              to: ref('dim_customers')
              field: customer_key
              severity: error
      - name: order_date
        description: date of the order
      - name: status_code
        description: status of the order
      - name: priority_code
        description: code associated with the order
      - name: clerk_name
        description: id of the clerk
      - name: ship_priority
        description: numeric representation of the shipping priority, zero being the default
      - name: order_count
        description: count of order
      - name: gross_item_sales_amount
        description: This is the extended price
      - name: item_discount_amount
        description: Discount amount related to the item
      - name: item_tax_amount
        description: Tax amount in dollars
      - name: net_item_sales_amount
        description: Gross item sales amount + discount amount + tax amount
